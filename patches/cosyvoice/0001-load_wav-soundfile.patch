diff --git a/cosyvoice/utils/file_utils.py b/cosyvoice/utils/file_utils.py
index b173ef2..8442b05 100644
--- a/cosyvoice/utils/file_utils.py
+++ b/cosyvoice/utils/file_utils.py
@@ -42,7 +42,16 @@ def read_json_lists(list_file):
 
 
 def load_wav(wav, target_sr, min_sr=16000):
-    speech, sample_rate = torchaudio.load(wav, backend='soundfile')
+    import soundfile as sf
+    try:
+        data, sample_rate = sf.read(wav, dtype='float32', always_2d=True)
+        speech = torch.from_numpy(data.T).to(dtype=torch.float32)
+    except Exception:
+        try:
+            speech, sample_rate = torchaudio.load(wav, backend='soundfile')
+        except Exception:
+            speech, sample_rate = torchaudio.load(wav)
+        speech = speech.to(dtype=torch.float32)
     speech = speech.mean(dim=0, keepdim=True)
     if sample_rate != target_sr:
         assert sample_rate >= min_sr, 'wav sample rate {} must be greater than {}'.format(sample_rate, target_sr)
