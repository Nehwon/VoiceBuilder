# .dockerignore dédié au Dockerfile de base (docker/base/Dockerfile).
# La base n'embarque AUCUN fichier du projet : uniquement Ubuntu + torch/CUDA.
# On exclut tout le contexte pour un build vide et rapide.
*